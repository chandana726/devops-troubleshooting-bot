apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-bot-chart.fullname" . }}
  labels:
    {{- include "devops-bot-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-bot-chart.selectorLabels" . | nindent 4 }}
