apiVersion: v1
kind: Service
metadata:
  name: devops-bot-service
spec:
  type: NodePort
  selector:
    app: devops-bot
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007